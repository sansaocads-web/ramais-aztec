services:
  ramais:
    build: .
    container_name: ramais-aztec
    restart: unless-stopped
    ports:
      - "3000:3000"
    volumes:
      - ramais_data:/app/data
    environment:
      - DATA_FILE=/app/data/ramais.json

volumes:
  ramais_data:
